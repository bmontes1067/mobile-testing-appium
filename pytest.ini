[pytest]
addopts =
    --html=reports/report.html
    --self-contained-html
    -v

markers =
    platform: marca el test con la plataforma objetivo (android | ios)
    smoke: tests de humo (subset mínimo para CI rápido)
    regression: suite de regresión completa

testpaths =
    tests
