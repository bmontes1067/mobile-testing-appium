name: 📱 Mobile Tests — Appium

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

defaults:
  run:
    shell: bash

jobs:

  # ─── Lint & collect on every push ─────────────────────────────────────────
  validate:
    name: Lint & Collect
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -r requirements.txt flake8

      - name: Collect tests (dry run)
        run: pytest --collect-only -q

      - name: Lint
        run: flake8 pages/ tests/ utils/ config/ --max-line-length=120 --ignore=E501,W503

  # ─── Android tests on emulator ────────────────────────────────────────────
  android:
    name: Android Tests
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python deps
        run: pip install -r requirements.txt

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: temurin

      - uses: android-actions/setup-android@v3

      - name: Install Appium
        run: |
          npm install -g appium
          appium driver install uiautomator2
          appium --log-level error &
          sleep 5

      - name: Download APK
        run: python scripts/download_apps.py --platform android

      - name: Start emulator and run tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          target: google_apis
          arch: x86_64
          profile: pixel_6
          script: >
            pytest tests/android/ --platform android -v
            --html=reports/report-android.html
            --self-contained-html

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: android-report
          path: reports/

  # ─── iOS tests on simulator ───────────────────────────────────────────────
  ios:
    name: iOS Tests
    runs-on: macos-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python deps
        run: pip install -r requirements.txt

      - name: Install Appium
        run: |
          npm install -g appium
          appium driver install xcuitest
          appium --log-level error &
          sleep 5

      - name: Download iOS app
        run: python scripts/download_apps.py --platform ios

      - name: Boot simulator
        run: |
          UDID=$(xcrun simctl list devices available | grep "iPhone 15 (" | head -1 | grep -oE '[A-F0-9-]{36}')
          xcrun simctl boot "$UDID" || true
          echo "SIMULATOR_UDID=$UDID" >> $GITHUB_ENV

      - name: Run iOS tests
        env:
          IOS_DEVICE_NAME: iPhone 15
          IOS_PLATFORM_VERSION: "17.0"
        run: >
          pytest tests/ios/ --platform ios -v
          --html=reports/report-ios.html
          --self-contained-html

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-report
          path: reports/
